export type TDenseEvent = {
  t?: string; // type
  c?: number; // timestamp
  l?: string; // location
  x?: number; // x
  y?: number; // y
  o?: boolean; // leave
  n?: string; // notes
  h?: boolean; // harmony
  s?: boolean; // spotlit
  r?: boolean; // trap
  m?: boolean; // miss
};
